# Xray Runtime Contract

Git stores sanitized templates only.

Do not commit UUIDs, private keys, short IDs, domains, or client subscription material.

Canonical template: runtime/templates/xray/config.json.template
