# nginx Runtime Contract

Git stores sanitized templates only.

Do not commit live domains, certificate private keys, subscription endpoints, or client material.

Canonical template: runtime/templates/nginx/sub.conf.template
